/** Image files the server decodes, and NIfTI volumes (.nii, .nii.gz): the accept list of file dialogs */
export const IMAGE_FILE_TYPES = '.png,.jpg,.jpeg,.bmp,.tif,.tiff,.dcm,.dicom,.nii,.gz,image/png,image/jpeg,image/bmp,image/tiff,application/dicom';
